# Jules Changelog

### [2025-05-20] - v1.0.0

#### Added
- Added comprehensive Neo-brutalism inspired CSS in `src/styles.css` for buttons, headers, and charts.
- Added `.jules/` directory with tracking files (`todo.md`, `knowledge.md`, `changelog.md`).

#### Changed
- Enabled "wide" layout mode in `src/dashboard/app.py`.
- Enabled CSS loading in `src/dashboard/app.py` with robust path handling.

**Files Modified:**
- `src/dashboard/app.py`
- `src/styles.css`
